# Example YAML-driven pipeline
input: ./examples/sample.jsonl
output: ./examples/out.chunked.jsonl
max_records: 100
stages:
  - name: agentinstruct
    params:
      k_variants: 2
  - name: chunk
    params:
      model_name: gpt-4o-mini
      max_tokens: 256
      overlap: 32
